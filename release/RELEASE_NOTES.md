PolyG EEG 취득 앱 (Windows x64). 필요한 런타임 DLL이 함께 들어 있어 **Windows 10 이상**에서는 별도 설치 없이 압축만 풀면 실행됩니다.

압축을 풀고 `Test_LXSM_D1WD10.exe`를 실행하세요. 폴더 안의 **모든 파일을 같은 위치에** 두어야 합니다(장비 DLL + 런타임 DLL).

사용 전 `polyg-bridge`를 먼저 실행해야 LSL로 데이터가 나갑니다 (zip 안의 `README.md` 참고).
